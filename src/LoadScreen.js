import React from "react";
import logo from "./logo.svg";

export default function LoadingScreen() {
  return (
    <div
      style={{
        background: "#fff",
        height: "100vh",
        width: "100vw",
        color: "#333",
      }}
    >
      Loading...
    </div>
  );
}
